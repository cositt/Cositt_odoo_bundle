<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="ir_cron_check_document_expiry" model="ir.cron">
        <field name="name">Cositt: Check employee document expiry</field>
        <field name="model_id" ref="model_hr_employee_document"/>
        <field name="state">code</field>
        <field name="code">model._cron_check_expiring_documents()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="active" eval="True"/>
    </record>

</odoo>
